<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD 3.0//EN"
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.flutterboard.base.dao.BoardDao">

	<!-- Desc: 메인화면 출력 -->
	<!-- Date: 2022-12-25 -->
	<select id="ShowMain"
		resultType="com.flutterboard.base.dto.BoardDto">
		select boardid, writerid, username, title, writedate, board.updatedate as updatedate
		from `user`, board
		where userid=writerid and board.deletedate is null and `user`.deletedate is null;
	</select>

	<!-- Desc: 게시글 상세보기 출력 -->
	<!-- Date: 2022-12-25 -->
	<select id="BoardDetail"
		resultType="com.flutterboard.base.dto.BoardDto">
		select title, username, writerid, writedate, board.content,
		board.updatedate from board, `user` where boardid=#{boardid} and
		writerid=userid;
	</select>
</mapper>