<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD 3.0//EN"
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.flutterboard.base.dao.BoardDao">

	<!-- Desc: 메인화면 출력 -->
	<!-- Date: 2022-12-25 -->
	<select id="ShowMain"
		resultType="com.flutterboard.base.dto.BoardDto">
		select boardid, writerid, username, title, writedate
		as writedate, date(board.updatedate) as updatedate, username as
		writername from `user`, board
		where userid=writerid and board.deletedate is null and `user`.deletedate is
		null;
	</select>
</mapper>